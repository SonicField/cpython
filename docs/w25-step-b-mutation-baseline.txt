=== BASELINE (pre-Step-B, dual-mutation .h+.cpp at HEAD 2ed74a92a8840b2f87d470740de44bd665872baf) ===
Mutation: hir_c_insert_before added 3rd arg (void *unused_drift_param) in BOTH .h (line 726) and .cpp (line 2349)

BUILD_EXIT=0  ← DRIFT UNDETECTED at compile time = §1b drift surface CONFIRMED

Pre-Step-B reality:
- hir_c_api.cpp compiles fine (header + impl agree on new sig)
- §1b TUs (licm_c.c + pass_output_type_c.c) use local extern decls (still 2-arg signature)
- Their callers compile fine against the local extern
- Linker resolves 'hir_c_insert_before' by name → finds 3-arg impl
- Result: silent runtime UB if these call sites execute (extra arg never passed; impl reads garbage)

Per theologian [chat 19:29Z] acceptance flip: BUILD_EXIT=0 = baseline PASS

Bonus pre-existing drift (no mutation needed): pass_output_type_c.c local externs use 'void *block' for hir_bb_*; canonical hir_c_api.h post-Step-A uses 'struct HirBasicBlock *block'. ABI-compat at 8 bytes silently links today.

=== POST-STEP-B (post-Step-B substantive close, dual-mutation .h+.cpp at HEAD ab0764195ee24b3b378192b54f7eac340da60fd3) ===
Mutation: hir_c_insert_before added 3rd arg (void *unused_drift_param) in BOTH .h (line 740) and .cpp (line 2349)

BUILD_EXIT=2  ← DRIFT CAUGHT at compile time = §1b drift surface CLOSED by Step B

Compile errors (6 call sites across 3 §1b TUs):
  Python/jit/hir/licm_c.c:244:50 — too few arguments to function call, expected 3, have 2
  Python/jit/hir/pass_output_type_c.c:981:53 — too few arguments to function call, expected 3, have 2
  Python/jit/hir/pass_output_type_c.c:989:48 — too few arguments to function call, expected 3, have 2
  Python/jit/hir/pass_output_type_c.c:1054:59 — too few arguments to function call, expected 3, have 2
  Python/jit/hir/simplify_c.c:40:57 — too few arguments to function call, expected 3, have 2
  Python/jit/hir/simplify_c.c:2829:54 — too few arguments to function call, expected 3, have 2

Post-Step-B reality:
- hir_c_api.cpp compiles header+impl-consistent on new sig
- §1b TUs (licm_c.c + pass_output_type_c.c + simplify_c.c) NO LONGER hold local externs (deleted in Step B-1/B-3/B-5)
- Each §1b TU now #includes hir_c_api.h directly → sees canonical 3-arg sig
- 2-arg call sites at the 6 listed locations fail with clear 'too few arguments' compile error
- Drift caught BEFORE link, no silent runtime UB possible

§5.3 acceptance per spec §5: PASS end-to-end (PRE-STEP-B BUILD_EXIT=0 + POST-STEP-B BUILD_EXIT≠0 with errors at expected §1b TUs).

Authorship: testkeeper executed procedure (chat L2327); generalist commits artifact per role boundary (push 67 precedent).

=== POST-STEP-B-FINAL (TYPE-ONLY mutation, dual .h+.cpp at HEAD dac46b963ea370005f9a58ebca11f26725fbe797) ===
Mutation: hir_c_insert_before 2nd arg HirInstr → struct HirBasicBlock* in BOTH .h (line 744) and .cpp (line 2349)
NO arity change (still 2 args). Pure void*-typedef drift class test.

BUILD_EXIT=0  ← DRIFT UNDETECTED at compile time = void*-typedef drift class REAL

C99 §6.3.2.3 reality:
- hir_c_api.cpp compiles fine (impl uses as_instr cast, void*→struct ptr legal)
- §1b TUs include hir_c_api.h, see canonical (struct HirBasicBlock *) sig
- Callers pass HirInstr (= void*); compiler implicitly converts void* → struct HirBasicBlock* with NO diagnostic
- Linker resolves silently → runtime UB if call sites execute (impl gets pointer mis-typed at the type-system level, but bits are identical because both are 8-byte pointers)

Per spec §5 acceptance interpretation (theologian L2371):
- BUILD_EXIT=2 (caught) = void*-typedef partially closed via canonicalization ← NOT this case
- BUILD_EXIT=0 (uncaught) = void*-typedef drift REAL = W25b (HirInstr/HirRegister/HirFunction → struct ptr promotion) REQUIRED ← THIS case

W25 outcome:
- ARITY drift on canonical-included paths: CLOSED (Step B + C)
- TYPE-ONLY drift on void*-aliased handles: OPEN — W25b required for full coverage

Authorship: testkeeper executed (chat L2382); generalist commits artifact per role boundary.
